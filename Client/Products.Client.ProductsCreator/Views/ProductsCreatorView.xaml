<Window x:Class="Products.Client.ProductsCreator.Views.ProductsCreatorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Title="ProductsCreatorView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Наименование" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name}" />
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Цена" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Count}" />
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Количество" />
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Price}" />
        <Button  Grid.Row="3" Grid.Column="1" Content="Создать" Command="{Binding CreateProductCommand}"/>
    </Grid>
</Window>
